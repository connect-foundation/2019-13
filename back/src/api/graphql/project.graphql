scalar Upload

type Query {
  projects: [project]
  findProjectById(projectId: String!): project
  findProjectsByUserId: [project]!
}
type Mutation {
  createProjectAndBlocks(projectTitle: String!, workspacesInput: [workspaceInput], images: [Upload]!): String!
  updateProjectAndBlocks(projectId: String!, projectTitle:String!, workspacesInput: [workspaceInput], images: [Upload]!): Boolean!
  deleteProjectAndBlocks(projectId: String!): Boolean!
}