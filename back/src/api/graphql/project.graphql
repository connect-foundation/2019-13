scalar Upload

type Query {
  projects: [project]
  findProjectById(projectId: String!): project
  findProjectsByUserId: [project]!
}
type Mutation {
  createProjectAndBlocks(projectTitle: String!, workspacesInput: [workspaceInput]): String!
  updateProjectAndBlocks(projectId: String!, projectTitle:String!, workspacesInput: [workspaceInput]): Boolean!
  deleteProjectAndBlocks(projectId: String!): Boolean!
}